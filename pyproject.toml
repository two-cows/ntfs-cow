[project]
name = "ntfs-cow"
version = "0.1.0"
description = "Development tooling for ntfs-cow"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit==4.6.2",
]

[tool.uv]
package = false
